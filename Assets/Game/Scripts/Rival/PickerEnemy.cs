using System.Collections;
using System.Collections.Generic;
using Game.Scripts.Core.Observer;
using UnityEngine;

public class PickerEnemy : Subject
{
    // Start is called before the first frame update
    protected override void Start()
    {
        base.Start();
    }
}
